## 2026-08 status

August 2026 work for Planner v1: playbooks, deterministic retrieval, constrained plans, and step execution.

### 2026-08 starting point

- July closed with capability-protected sandbox execution, deterministic tool-outcome replay, and a two-scenario golden evaluation set.
- The April local-model spike showed that raw planner output was not reliable enough to trust: it violated requested formats, hallucinated repository structure, and drifted away from the .NET architecture.
- Manifest schema `0.3` and event-log schema `0.2` did not contain planner artifacts.
- No playbook store, constrained plan contract, or planner validator existed.

### 2026-08 first slice

Implemented the deterministic planner foundation before model generation or step execution:

- Added separate versioned planner contracts:
  - plan schema `0.1`
  - playbook schema `0.1`
  - ordered plan steps with explicit ids, action ids, and arguments
  - allowed-action definitions bound to a concrete tool id/version and argument allow-list
  - playbook step templates and explicit retrieval terms
- Added a fail-closed `PlannerPlanValidator`:
  - rejects unsupported plan schema versions
  - requires plan identity, task class, and at least one step
  - requires contiguous one-based step ordering
  - rejects duplicate step ids
  - matches action ids using ordinal, case-sensitive comparison
  - rejects actions and arguments not present in the allowed-action catalogue
  - enforces required action arguments
  - returns stable machine-readable error codes and paths
- Added `IPlaybookStore` and `InMemoryPlaybookStore`:
  - validates playbook schema and required fields at startup
  - rejects duplicate playbook id/version pairs
  - snapshots mutable input collections
  - normalizes retrieval terms using trim + invariant lowercase
  - returns playbooks in stable task-class/id/version order
- Added `DeterministicPlaybookRetriever`:
  - exact ordinal task-class filtering
  - exact normalized retrieval-term overlap scoring
  - score-descending ranking
  - lexical playbook-id/version tie-breaking
  - query/store input order does not affect results
  - zero-overlap candidates are excluded when query terms are supplied
- Added focused tests for plan validation, fail-closed action matching, deterministic error ordering, retrieval ranking, order independence, result limits, store snapshots, duplicate identities, and unsupported schemas.

### August 1/2 verification

```bash
scripts/ci-local.sh --fast
```

Result:

- Release build succeeded with 0 errors.
- 112 tests passed, 0 failed, and 6 opt-in container tests were skipped.
- The 12 new planner/retrieval tests passed.
- The existing `Microsoft.OpenApi 2.4.1` high-severity advisory remains visible and is unchanged by this slice.

### Planner artifact decision

- Manifest schema `0.3` remains unchanged.
- Treat plans and playbooks as separate schema-versioned contracts, both currently `0.1`.
- The validated `PlannerPlanEvent` is persisted as the `planner.plan` domain payload inside the HMAC-chained event log `0.2`.
- The event contains the normalized task, candidate source, selected playbook identity/score, allowed-action catalogue, validated plan, and validation result.
- Validation happens before execution, signing, or persistence.
- The signed manifest binds the planner event through the existing event-log record count and tail chain MAC, so no manifest field or version change is required.
- Replay validates and consumes the recorded plan and recorded tool outcomes; it does not rerun retrieval, model generation, or external tools.

### August 2/2 closure scenarios

The end-of-month vertical slice will include at least these five successful scenarios:

1. Exact task-class and term retrieval produces and executes a one-step plan.
2. A two-step plan executes strictly in sequence and records both outcomes.
3. Multiple playbooks with different overlap scores select the highest-scoring playbook and execute its plan.
4. Equally scored playbooks use the lexical tie-break and execute the same selected plan regardless of input order.
5. A plan using both required and optional allowed arguments reaches the capability-protected tool executor successfully.

Negative coverage remains additional to the five successful scenarios:

- unsupported or malformed plan schema
- unknown action or argument
- capability denial
- sandbox timeout/failure
- local-model output that cannot be parsed or validated

### August 2/2 completed work

- Added `DeterministicPlannerService` for normalized task -> deterministic retrieval -> candidate materialization -> fail-closed validation.
- Added exact `{{argument}}` playbook-template binding with stable missing-binding failures.
- Added `IPlannerCandidateProvider` and an optional `LocalModelPlanCandidateProvider`; raw model JSON is parsed as an untrusted candidate and passes through the same validator.
- Added `AllowedActionCatalog` as the shared immutable mapping from action ids to concrete tool ids/versions and allowed arguments.
- Added `PlannerStepExecutor`:
  - stable per-step invocation ids
  - canonical ordinal argument serialization
  - one JWT capability per invocation
  - strict sequential execution through `IToolExecutor`
  - stop-on-first denial, timeout, protocol failure, or other non-success result
  - deterministic cancellation before the next step
- Added `POST /workflow/planner` with configuration-backed actions and playbooks.
- Added signed `planner.plan`, per-step `tool.execution`, and `workflow.planner` evidence.
- Added planner replay with recorded plan validation, recorded capability decisions, and recorded tool results.
- Added the five successful planner golden scenarios and negative unit coverage for no retrieval match, missing template values, malformed model JSON, task-class mismatch, disallowed actions, capability denial, cancellation, timeout, and sandbox failure.

### August closure verification

```bash
dotnet restore source/Aos.sln --disable-build-servers
dotnet test source/Aos.sln --no-restore -c Release -v minimal --disable-build-servers -m:1
AOS_CONTAINER_TESTS=1 dotnet test source/Aos.sln --no-restore -v minimal --disable-build-servers -m:1
dotnet run --project source/Aos.ReplayCli -c Release --no-build -- evaluate --scenarios-root source/Aos.WebApi.Tests/Golden --hmac-key golden-hmac-key
dotnet list source/Aos.sln package --include-transitive --vulnerable --no-restore
```

Results:

- Release suite: 126 passed, 0 failed, 6 opt-in container tests skipped.
- Container-enabled suite: 134 passed, 0 failed, 0 skipped.
- Golden evaluation: 7 scenarios passed, including all five planner scenarios.
- Live `POST /workflow/planner` smoke execution succeeded through a warm `container-v1` slot, wrote signed artifacts, and replay verified those artifacts.
- No sandbox containers remained after verification.
- NuGet advisory scan reported no vulnerable packages.
- Updated stable packages include `Microsoft.AspNetCore.OpenApi 10.0.11`, `Swashbuckle.AspNetCore 10.2.3`, `Microsoft.Extensions.Options 10.0.11`, aligned OpenTelemetry `1.18.0`, `Scalar.AspNetCore 2.17.2`, `Serilog.Settings.Configuration 10.0.1`, and `Microsoft.NET.Test.Sdk 18.9.0`.

### August status

August Planner v1 is complete. Five representative tasks plan and execute end-to-end with signed logs/manifests, and replay reproduces the recorded planner and tool outcomes.

### Scope boundary

- Model-based critic scoring, backtracking, alternative-plan retrieval, step repair, and recovery loops remain September Planner v2 work.
- Remote CI remains deferred.
- The `Microsoft.OpenApi` advisory was resolved during closure; `Aos.Core` namespace cleanup remains separate technical-debt work.
