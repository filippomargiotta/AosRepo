## 2026-03 status
High-level progress for March 2026 (replay hardening).

2026-03 update (DONE)
Implemented Chunk 1 replay CLI workflow routing:
- Added explicit `--workflow` selection to `Aos.ReplayCli`.
- Introduced a workflow handler registry so replay is no longer hardcoded directly in `ReplayCliRunner`.
- Moved the existing `hello` replay path behind a dedicated workflow adapter.
- Preserved deterministic manifest comparison and event log byte comparison behavior.
- Added CLI tests for:
  - successful `hello` replay via `--workflow hello`
  - missing `--workflow`
  - unknown workflow
  - existing mismatch / invalid input paths
- Test suite now passes with 21 total tests.

2026-03 update (DONE)
Implemented Chunk 2 replay mismatch reporting:
- Replaced coarse manifest mismatch messages with field-level diffs.
- Added event log mismatch output that identifies the exact line and field that changed.
- Kept replay-specific `timeSource` differences out of deterministic manifest comparisons.
- Preserved event log byte comparison as a fallback when no structural diff is found.
- Added CLI coverage for:
  - field-level manifest mismatch output
  - line/field event log mismatch output
- Test suite now passes with 22 total tests.

2026-03 update (DONE)
Implemented a narrow Chunk 3 schema/versioning baseline:
- Centralized the supported manifest/event-log schema version baseline at `0.1`.
- Tightened manifest validation to reject unsupported manifest versions.
- Added nested manifest validation for model/tool/policy entries instead of only checking list presence.
- Added explicit manifest/event-log compatibility checks:
  - event log `runId` must match manifest `runId`
  - event payload `manifestVersion`, when present, must match the manifest version
- Documented the current exact-version replay policy and no-migration expectation.
- Test suite now passes with 26 total tests.

2026-03 update (DONE)
Implemented March Step 1: HMAC-chained event-log integrity:
- Introduced signed event-log records with:
  - `schemaVersion`
  - wrapped `entry`
  - `integrity.algorithm`
  - `integrity.keyId`
  - `integrity.previousChainMac`
  - `integrity.chainMac`
- Bumped the event-log schema baseline from `0.1` to `0.2`.
- Added `HmacEventLogIntegrityChain` so record generation and validation are centralized.
- Updated workflow artifact generation and file writing to persist signed records instead of raw entries.
- Updated replay CLI to require `--hmac-key` and reject the first broken line before replay if the chain is tampered.
- Added tests for:
  - valid chain verification
  - tampered payload detection
  - removed line detection
  - reordered line detection
  - wrong key detection
  - replay CLI invalid chain handling
- Test suite now passes with 32 total tests.

2026-03 update (DONE)
- Implemented March Step 2 signed-manifest + evaluation slice:
  - Wrapped `manifest.json` in a signed artifact with `integrity.algorithm`, `integrity.keyId`, and `integrity.manifestMac`
  - Bumped the manifest schema baseline from `0.1` to `0.2`
  - Added `manifest.eventLog` summary fields so the signed manifest binds to the exact event-log chain
  - Added replay-time manifest integrity verification before event-log validation and workflow replay
  - Tightened artifact compatibility checks so manifest event-log summary and actual log tail/count must match
  - Updated replay to reproduce the recorded manifest exactly instead of tolerating replay-only `timeSource` differences
  - Added tests for:
    - valid manifest signing and verification
    - tampered manifest detection
    - manifest/event-log binding mismatch detection
    - signed golden artifact replay equivalence
- Implemented evaluation harness MVP:
  - Added `evaluate --scenarios-root <path> --hmac-key <value>` mode to `Aos.ReplayCli`
  - Added `scenario.json` metadata for golden scenarios
  - Added harness tests for pass/fail summaries over the golden set
- Added replay CLI artifact directory mode and clearer usage/exit-code help text
- Test suite now passes with 39 total tests.

2026-03 plan (PENDING)
- Expand golden coverage:
  - add another golden scenario beyond `hello`
