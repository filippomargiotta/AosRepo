## 2026-04 status
Initial April 2026 progress for capability router v1.

2026-04 update (DONE)
Implemented April Chunk 1 router foundations:
- Added a deterministic router scoring model over:
  - latency
  - cost
  - quality
  - compliance
- Added a simple deterministic constraint satisfaction pass before scoring:
  - `maxLatencyMs`
  - `maxCostPer1KTokens`
  - `minQualityScore`
  - `requiredComplianceTags`
- Added stable tie-breaking that does not depend on candidate list order:
  - score descending
  - compliance descending
  - quality descending
  - latency ascending
  - cost ascending
  - provider/model/version lexical order
- Added config-driven router candidates and weights in `appsettings.json`
- Added `POST /router/decide` so the routing decision can be exercised through the API
- Added test coverage for:
  - highest-score selection
  - deterministic constraint filtering
  - order-independent deterministic output
  - stable tie-breaking
  - no-match behavior with explicit rejections
- Test suite now passes with 44 total tests.

2026-04 update (DONE)
Implemented April Chunk 2 router integration:
- Integrated router selection into the `hello` workflow artifact path.
- Manifest schema baseline is now `0.3` so signed manifests include:
  - effective router policy
  - effective constraints and weights
  - selected model
  - ranked candidates and scores
  - rejection reasons
- Replay uses the recorded routing decision from the manifest instead of live router configuration, preserving historical replay determinism.
- Added task-class-aware router policy variants in configuration.
- Router now merges request constraints with task-class policy defaults using stricter deterministic constraints:
  - lower max latency
  - lower max cost
  - higher min quality
  - union of required compliance tags
- Refreshed golden artifacts to manifest version `0.3`.
- Added test coverage for:
  - workflow manifests using the router-selected model
  - no-route workflow failure
  - manifest validation for missing route decisions
  - policy defaults
  - stricter request/policy constraint merging
- Test suite now passes with 48 total tests.

2026-04 update (DONE)
Closed April with the early local planning spike:
- Used the host-local Ollama runtime with `qwen2.5-coder:1.5b-base` (`1.5B`, `Q4_K_M`) at temperature `0`.
- Ran 5 representative planning tasks:
  - router benchmark suite
  - second golden replay scenario
  - planner schema decision
  - README routing docs update
  - sandbox tool execution protocol
- Observed that the model can emit plan-like continuations, but reliability is low:
  - frequently ignored the requested `PLAN` / `RISKS` structure
  - hallucinated repo paths, endpoints, files, and commands
  - drifted toward Python / YAML artifacts instead of the existing .NET codebase
  - produced shallow or generic risk analysis
- Captured the task-by-task spike notes in `notes/2026-04-28-planning-spike.md`.
- Decision: keep planner output experimental for now and do not add it to manifest schema `0.3`.
- April can be treated as closed with router v1 code-complete and the local planner risk explicitly characterized.

2026-04 evidence
- Router implementation:
  - deterministic scoring, filtering, and tie-breaking in `source/Aos.WebApi/Services/DeterministicRouterService.cs`
  - workflow integration in `source/Aos.WebApi/Services/HelloWorkflowService.cs`
  - config-driven candidates and policies in `source/Aos.WebApi/appsettings.json`
- Router API surface:
  - `POST /router/decide` in `source/Aos.WebApi/Controllers/RouterController.cs`
- Replay/schema integration:
  - manifest schema baseline `0.3` with routing decisions recorded for replay
  - schema/versioning notes in `project/SchemaVersioning.md`
- Verification:
  - `dotnet test source/Aos.sln --no-restore -v minimal`
  - passing state at April closure: 48 tests
- Planning spike evidence:
  - detailed run log in `notes/2026-04-28-planning-spike.md`
  - 5 task samples captured
  - cold response latency about `10.1s`
  - warm response latency about `1.5s` to `2.1s`

2026-04 technical conclusion
- Deterministic capability routing is implemented, integrated into workflow artifacts, and replay-safe.
- Small local planning models can produce plan-like continuations in this environment, but repo-grounded reliability is currently insufficient for signed planner artifacts.
- Manifest schema `0.3` should remain router-focused; planner artifacts should wait for a later dedicated schema slice plus stronger structural validation and a more reliable local planning model.

2026-04 outcome
- Treat router v1 as code-complete for April.
- Keep local planning work separate from replay artifacts until a later dedicated planner schema slice and a more reliable local planning setup exist.
