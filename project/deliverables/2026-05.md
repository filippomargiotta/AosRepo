## 2026-05 status
Initial May 2026 planning for router v2 + metrics store.

2026-05 starting point
- Shared replay/artifact/workflow logic now lives in `source/Aos.Core`.
- `Aos.ReplayCli` no longer depends directly on `Aos.WebApi`.
- Router internals are split into:
  - compiled router catalog / policy snapshot
  - deterministic selection engine
- Test support has been consolidated while preserving the 48-test baseline.

2026-05 plan (NEXT)
- Add a router benchmark suite that reports routing latency, including p95.
- Add a small metrics store keyed by model + task class for historical performance weighting.
- Preserve deterministic routing behavior under candidate order variation and concurrent access.
- Measure the current routing latency baseline before optimization work.

2026-05 notes
- Keep planner artifacts out of manifest/replay schema work for now; April's local planning spike was informative but not stable enough for signed artifacts.
- The remaining March stretch goal of another golden replay scenario can stay secondary unless it directly supports router benchmarking or metrics validation.

2026-05 definition of done
- Benchmark reporting shows router p95 decision latency.
- Routing decisions remain stable across reorderings and replay scenarios.
- A minimal metrics shape exists for later weighting work without destabilizing current replay artifacts.
